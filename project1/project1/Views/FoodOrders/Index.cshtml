@model IEnumerable<project1.data.Entities.FoodOrder>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Ordertime)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NameNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Ordertime)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NameNavigation.Name)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Ordernum">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Ordernum">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Ordernum">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
